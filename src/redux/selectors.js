export const selectQuizList = (state) => state.quiz.quizList;
export const selectQuizElement = (state) => state.quiz.quizElement;
export const selectLoading = (state) => state.quiz.isLoading;
export const selectError = (state) => state.quiz.errorMessage;
